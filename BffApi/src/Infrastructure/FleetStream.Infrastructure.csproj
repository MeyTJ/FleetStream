<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>FleetStream.Infrastructure</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Core\FleetStream.Core.csproj" />
    <ProjectReference Include="..\Application\FleetStream.Application.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Confluent.Kafka" Version="2.6.0" />
    <PackageReference Include="Microsoft.AspNetCore.SignalR.StackExchangeRedis" Version="10.0.11" />
    <PackageReference Include="Microsoft.Extensions.Caching.StackExchangeRedis" Version="10.0.0" />
    <!--
      Microsoft.Extensions.Diagnostics.HealthChecks, .Hosting.Abstractions and
      .Options are intentionally NOT referenced: the Microsoft.AspNetCore.App
      framework reference above already provides them, and listing them triggers
      NU1510 ("will not be pruned ... likely unnecessary").
    -->
    <PackageReference Include="StackExchange.Redis" Version="2.8.16" />
  </ItemGroup>

  <!--
    graphify-dotnet-core: Code-base knowledge-graph generation.
    Provides isolated per-project dependency graphs, exported as JSON / HTML / report.
    Wrapped entirely by GraphGeneratorService so Application layer stays decoupled.
  -->
  <ItemGroup>
    <PackageReference Include="graphify-dotnet-core" Version="0.7.0" />
  </ItemGroup>

</Project>
