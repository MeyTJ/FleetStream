# Next.js frontend — copy to .env.local

# BFF API base URL (REST)
NEXT_PUBLIC_BFF_URL=http://localhost:8080

# SignalR hub URL (WebSocket)
NEXT_PUBLIC_SIGNALR_HUB=http://localhost:8080/hubs/v1/fleet

# Environment label (dev | staging | production)
NEXT_PUBLIC_APP_ENV=dev
